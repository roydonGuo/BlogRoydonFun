import { ssrRenderAttrs, ssrRenderAttr, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const _imports_0 = "/images/posts/redis-persistence-rdb-aof-engineering-knowledge-map.png";
const __pageData = JSON.parse('{"title":"Redis 持久化工程实践：RDB、AOF、重写与故障恢复","description":"","frontmatter":{"title":"Redis 持久化工程实践：RDB、AOF、重写与故障恢复","date":"2026-08-12T00:00:00.000Z","category":"Redis","cover":"/covers/backend.svg","tags":["redis","rdb","aof"],"excerpt":"从数据安全目标出发，讲清 RDB 快照、AOF 写回与 Redis 7.0+ 多文件重写机制，并把大 Key、磁盘故障、监控告警和恢复演练串成一套可落地的持久化方案。"},"headers":[],"relativePath":"posts/redis-persistence-rdb-aof-engineering.md","filePath":"posts/redis-persistence-rdb-aof-engineering.md","lastUpdated":1787128073000}');
const _sfc_main = { name: "posts/redis-persistence-rdb-aof-engineering.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="redis-持久化工程实践-rdb、aof、重写与故障恢复" tabindex="-1">Redis 持久化工程实践：RDB、AOF、重写与故障恢复 <a class="header-anchor" href="#redis-持久化工程实践-rdb、aof、重写与故障恢复" aria-label="Permalink to “Redis 持久化工程实践：RDB、AOF、重写与故障恢复”">​</a></h1><img${ssrRenderAttr("src", _imports_0)} alt="Redis 持久化工程实践：RDB、AOF、重写与故障恢复知识串联图" style="${ssrRenderStyle({ "border-radius": "10px" })}"><p>从数据安全目标出发，讲清 RDB 快照、AOF 写回与 Redis 7.0+ 多文件重写机制，并把大 Key、磁盘故障、监控告警和恢复演练串成一套可落地的持久化方案。</p><h2 id="先说结论-持久化不是一个开关-而是一组恢复承诺" tabindex="-1">先说结论：持久化不是一个开关，而是一组恢复承诺 <a class="header-anchor" href="#先说结论-持久化不是一个开关-而是一组恢复承诺" aria-label="Permalink to “先说结论：持久化不是一个开关，而是一组恢复承诺”">​</a></h2><p>Redis 是内存数据库，持久化解决的是进程退出或机器故障后如何重建数据集。工程上不能只问“有没有开启 AOF”，而要先定义三件事：</p><ul><li><strong>RPO（恢复点目标）</strong>：最多允许丢失多长时间的数据；</li><li><strong>RTO（恢复时间目标）</strong>：故障后多久必须恢复服务；</li><li><strong>故障范围</strong>：只防进程崩溃，还是还要防磁盘损坏、误删除和整个可用区故障。</li></ul><p>Redis Open Source 提供四种持久化组合：不持久化、只用 RDB、只用 AOF、同时使用 RDB 与 AOF。它们没有脱离业务语义的“最佳答案”：纯缓存可以不持久化；能从数据库重建但希望缩短预热时间的缓存适合 RDB；不能轻易重建的会话、计数或任务状态通常需要 AOF；重要数据还需要异机备份和恢复演练，不能把本机持久化文件当成备份。</p><p>本文以 Redis 7.0+ 的多文件 AOF 为主要版本边界，并用 Redis 当前官方文档与 Redis 8.8 命令参考核对，事实核对日期为 2026-08-12。Redis 7.0 以前的单文件 AOF 在目录结构、重写期间内存和写入路径上有所不同，运维方案不能直接照搬。</p><h2 id="一、四种持久化策略如何选择" tabindex="-1">一、四种持久化策略如何选择 <a class="header-anchor" href="#一、四种持久化策略如何选择" aria-label="Permalink to “一、四种持久化策略如何选择”">​</a></h2><table tabindex="0"><thead><tr><th>策略</th><th>数据恢复来源</th><th style="${ssrRenderStyle({ "text-align": "right" })}">典型 RPO</th><th>启动特征</th><th>适用场景</th></tr></thead><tbody><tr><td>不持久化</td><td>上游系统重新回填</td><td style="${ssrRenderStyle({ "text-align": "right" })}">取决于上游</td><td>无持久化文件加载</td><td>可丢弃且可快速重建的纯缓存</td></tr><tr><td>只用 RDB</td><td>最近一次时间点快照</td><td style="${ssrRenderStyle({ "text-align": "right" })}">通常为分钟级</td><td>文件紧凑，较适合备份与快速恢复</td><td>可接受一段数据丢失、重视恢复速度</td></tr><tr><td>只用 AOF</td><td>重放写命令或加载 AOF 基础文件</td><td style="${ssrRenderStyle({ "text-align": "right" })}">取决于 <code>appendfsync</code></td><td>文件通常更大，恢复工作更多</td><td>希望缩小进程故障的数据丢失窗口</td></tr><tr><td>RDB + AOF</td><td>重启时优先使用 AOF</td><td style="${ssrRenderStyle({ "text-align": "right" })}">通常由 AOF 决定</td><td>同时承担快照、追加和重写开销</td><td>数据重要，需要更完整的本机恢复手段</td></tr></tbody></table><p>同时开启 RDB 与 AOF 时，Redis 重启会使用 AOF 恢复数据，因为 AOF 通常保存更完整的变更历史。RDB 仍然有价值：它是紧凑的时间点文件，适合异地备份、版本留存和更快的数据集装载。</p><p>这里要特别区分三个概念：</p><ol><li><strong>持久化</strong>把内存状态落到本机耐久介质；</li><li><strong>复制</strong>把数据传播到其他 Redis 节点，主要服务于高可用和读扩展；</li><li><strong>备份</strong>保留独立、可回退、最好跨故障域的数据副本。</li></ol><p>主从复制不能代替备份。误执行 <code>FLUSHALL</code>、错误写入或逻辑删除也可能被复制到从节点；本机 RDB 和 AOF 又可能与机器或磁盘一起损坏。真正的数据安全需要把三者组合起来。</p><h2 id="二、rdb-把某个时间点的数据集做成快照" tabindex="-1">二、RDB：把某个时间点的数据集做成快照 <a class="header-anchor" href="#二、rdb-把某个时间点的数据集做成快照" aria-label="Permalink to “二、RDB：把某个时间点的数据集做成快照”">​</a></h2><p>RDB 将某个时间点的数据集编码为紧凑的二进制文件，默认文件名通常是 <code>dump.rdb</code>。它有三种常见触发方式：</p><ul><li><code>SAVE</code>：前台同步保存，执行期间阻塞服务处理请求，生产环境通常不应随意使用；</li><li><code>BGSAVE</code>：创建后台子进程生成快照，父进程继续服务客户端；</li><li><code>save &lt;seconds&gt; &lt;changes&gt;</code>：满足时间与变更次数条件时自动触发后台保存。</li></ul><p>一个 <code>BGSAVE</code> 的主流程是：</p><div class="language-text line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span>主进程 fork 子进程</span></span>
<span class="line"><span>        ↓</span></span>
<span class="line"><span>子进程读取 fork 时刻的数据视图</span></span>
<span class="line"><span>        ↓</span></span>
<span class="line"><span>写入临时 RDB 文件</span></span>
<span class="line"><span>        ↓</span></span>
<span class="line"><span>写完后原子替换旧 RDB</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><h3 id="_1-copy-on-write-为什么既高效又有风险" tabindex="-1">1. Copy-on-Write 为什么既高效又有风险 <a class="header-anchor" href="#_1-copy-on-write-为什么既高效又有风险" aria-label="Permalink to “1. Copy-on-Write 为什么既高效又有风险”">​</a></h3><p><code>fork()</code> 后父子进程最初共享物理内存页。子进程读取旧视图并写 RDB；父进程如果修改某个共享页，操作系统才为该页创建副本，这就是 Copy-on-Write（COW）。因此，后台保存不是把整个数据集同步复制一遍后才继续提供服务。</p><p>但 COW 不是“免费快照”：</p><ul><li>数据集越大，<code>fork()</code> 复制页表的成本越高，主线程暂停越明显；</li><li>快照期间写流量越大，被复制的内存页越多，额外内存压力越大；</li><li>大 Key 往往跨越许多内存页，更新少量业务字段也可能放大 COW；</li><li>磁盘写入慢会延长子进程存活时间，从而扩大 COW 发生窗口。</li></ul><p>所以容量规划不能让 <code>used_memory</code> 长期贴近机器物理内存。必须给页表、内存碎片、复制缓冲区、客户端缓冲区和持久化期间的 COW 留出余量。</p><h3 id="_2-rdb-的优势与边界" tabindex="-1">2. RDB 的优势与边界 <a class="header-anchor" href="#_2-rdb-的优势与边界" aria-label="Permalink to “2. RDB 的优势与边界”">​</a></h3><p>RDB 的优势是文件紧凑、适合复制和归档，并且大数据集恢复通常比逐条重放纯命令 AOF 更快。它的核心边界也很明确：两次快照之间的变更在意外停机时可能丢失。因此，不能把“每 5 分钟生成快照”解释成“绝不超过 5 分钟数据丢失”，还要考虑最后一次快照是否成功、文件是否完成转移、恢复时能否读取。</p><h2 id="三、aof-记录写命令-再重放得到数据集" tabindex="-1">三、AOF：记录写命令，再重放得到数据集 <a class="header-anchor" href="#三、aof-记录写命令-再重放得到数据集" aria-label="Permalink to “三、AOF：记录写命令，再重放得到数据集”">​</a></h2><p>AOF（Append Only File）记录会改变数据集的命令。客户端写命令执行后，数据先进入 Redis 的 AOF 缓冲，再通过 <code>write()</code> 进入操作系统页缓存；何时调用 <code>fsync()</code> 把数据推进耐久存储，由 <code>appendfsync</code> 策略决定。</p><div class="language-text line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span>客户端写命令</span></span>
<span class="line"><span>    ↓</span></span>
<span class="line"><span>Redis 执行并更新内存</span></span>
<span class="line"><span>    ↓</span></span>
<span class="line"><span>AOF 缓冲区</span></span>
<span class="line"><span>    ↓ write()</span></span>
<span class="line"><span>操作系统页缓存</span></span>
<span class="line"><span>    ↓ fsync()</span></span>
<span class="line"><span>耐久存储</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><h3 id="_1-三种-aof-写回策略" tabindex="-1">1. 三种 AOF 写回策略 <a class="header-anchor" href="#_1-三种-aof-写回策略" aria-label="Permalink to “1. 三种 AOF 写回策略”">​</a></h3><table tabindex="0"><thead><tr><th>配置</th><th>行为</th><th>数据安全</th><th>延迟与吞吐</th></tr></thead><tbody><tr><td><code>appendfsync always</code></td><td>每批追加后执行 <code>fsync</code></td><td>丢失窗口最小</td><td>磁盘延迟更容易进入请求路径，代价最高</td></tr><tr><td><code>appendfsync everysec</code></td><td>通常每秒由后台执行 <code>fsync</code></td><td>灾难时可能丢失约一秒写入</td><td>官方建议且默认的折中策略</td></tr><tr><td><code>appendfsync no</code></td><td>Redis 不主动 <code>fsync</code>，交给操作系统</td><td>丢失窗口由内核刷盘行为决定</td><td>性能较好，但耐久性最弱</td></tr></tbody></table><p><code>always</code> 不是业务“零数据丢失”的同义词。Redis 返回成功之后，控制器缓存、磁盘设备、虚拟化存储或故障域仍可能影响最终结果；跨节点复制也有自己的确认语义。支付、账务等强一致数据仍应以事务数据库为事实源，Redis 只承担缓存、加速或可重建的派生状态。</p><h3 id="_2-redis-7-0-的多文件-aof" tabindex="-1">2. Redis 7.0+ 的多文件 AOF <a class="header-anchor" href="#_2-redis-7-0-的多文件-aof" aria-label="Permalink to “2. Redis 7.0+ 的多文件 AOF”">​</a></h3><p>从 Redis 7.0 开始，AOF 由单文件演进为多文件结构：</p><ul><li><strong>Base AOF</strong>：最多一个，表示最近一次重写时的数据基础，可使用 RDB 或 AOF 格式；</li><li><strong>Incremental AOF</strong>：一个或多个，保存 Base 生成后持续到来的增量命令；</li><li><strong>Manifest</strong>：记录哪些 Base 和增量文件共同组成当前有效 AOF。</li></ul><p>这些文件位于 <code>appenddirname</code> 指定的目录中。重写时，父进程会打开新的增量文件继续承接写入；子进程生成新的 Base；完成后通过临时 Manifest 和原子替换让新文件集合生效。这个版本边界很重要：旧教程里“重写期间把全部增量同时保存在内存缓冲，最后一次性追加到新 AOF”的描述主要针对 Redis 7.0 以前，不能原样描述当前机制。</p><h2 id="四、aof-重写不是压缩日志-而是重建最短状态" tabindex="-1">四、AOF 重写不是压缩日志，而是重建最短状态 <a class="header-anchor" href="#四、aof-重写不是压缩日志-而是重建最短状态" aria-label="Permalink to “四、AOF 重写不是压缩日志，而是重建最短状态”">​</a></h2><p>AOF 会随着写命令不断增长。例如同一个计数器执行 100 次 <code>INCR</code>，恢复最终状态并不一定需要重放全部 100 条历史命令。<code>BGREWRITEAOF</code> 根据当前内存数据生成能恢复当前状态的更短表示，再切换到新 AOF 文件集合。</p><p>重写有三个工程意义：</p><ol><li>降低 AOF 占用空间；</li><li>减少启动恢复需要处理的数据量；</li><li>清除已经被后续写入覆盖的历史操作。</li></ol><p>它也会带来资源竞争：<code>fork()</code> 暂停、COW 内存、子进程 CPU、磁盘读写和新文件切换。不要把自动重写阈值设置得过于激进，也不要让 RDB 快照、AOF 重写、宿主机备份和批量任务同时争抢磁盘。Redis 会避免让 <code>BGSAVE</code> 与 AOF 重写两个重型后台持久化任务并行，但应用侧和基础设施侧的 I/O 峰值仍需自行错峰。</p><p>下面是一份面向 Redis 7.0+ 的示意配置，数值必须通过真实写流量、磁盘基线和 RPO 压测调整：</p><div class="language-conf line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">conf</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span># 开启 AOF；重要业务通常仍保留 RDB 作为独立时间点备份来源。</span></span>
<span class="line"><span>appendonly yes</span></span>
<span class="line"><span></span></span>
<span class="line"><span># 每秒刷盘是官方默认折中；若业务选择 always，必须先验证尾延迟。</span></span>
<span class="line"><span>appendfsync everysec</span></span>
<span class="line"><span></span></span>
<span class="line"><span># AOF 目录在 dir 配置目录下；Redis 7.0+ 在其中维护 Base、增量文件和 Manifest。</span></span>
<span class="line"><span>appenddirname &quot;appendonlydir&quot;</span></span>
<span class="line"><span></span></span>
<span class="line"><span># 当前 AOF 相对上次重写后的基础体积增长 100% 且达到最小体积后，才自动重写。</span></span>
<span class="line"><span>auto-aof-rewrite-percentage 100</span></span>
<span class="line"><span>auto-aof-rewrite-min-size 256mb</span></span>
<span class="line"><span></span></span>
<span class="line"><span># 保留 RDB 快照：900 秒内至少发生 1 次变更时生成快照。</span></span>
<span class="line"><span># 这里只是示例，不代表通用的 RPO 承诺。</span></span>
<span class="line"><span>save 900 1</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br></div></div><h2 id="五、大-key-为什么会放大持久化风险" tabindex="-1">五、大 Key 为什么会放大持久化风险 <a class="header-anchor" href="#五、大-key-为什么会放大持久化风险" aria-label="Permalink to “五、大 Key 为什么会放大持久化风险”">​</a></h2><p>大 Key 不只是“单条命令慢”，它会同时影响持久化的多个阶段：</p><ul><li><strong>主线程执行成本</strong>：读取、序列化、删除或更新大对象可能阻塞其他命令；</li><li><strong>COW 放大</strong>：<code>BGSAVE</code> 或 AOF 重写期间修改大对象，会复制更多内存页；</li><li><strong>fork 成本</strong>：总数据集越大，页表越大，<code>fork()</code> 越慢；</li><li><strong>AOF 与网络放大</strong>：大写命令增大 AOF 写入量，也可能推高复制缓冲和网络流量；</li><li><strong>恢复时间</strong>：大对象的加载、内存分配和重放会拉长 RTO。</li></ul><p>治理要从模型设计开始：把超大 Hash、List、Set 或字符串按业务维度拆成有界 Key；设置元素数量与序列化后字节上限；删除大 Key 时优先评估 <code>UNLINK</code>，把内存回收移到后台线程；用 <code>MEMORY USAGE</code>、<code>SCAN</code> 配合采样工具定位大 Key，但不要在生产上无边界执行 <code>KEYS *</code>。</p><p>需要注意，拆 Key 不是越碎越好。大量小 Key 会增加对象、字典和过期元数据开销，还会改变原子操作边界。正确做法是给单 Key 大小、集合元素数、单命令处理量和业务事务边界同时设预算。</p><h2 id="六、真实项目-订单状态缓存应该怎样落地" tabindex="-1">六、真实项目：订单状态缓存应该怎样落地 <a class="header-anchor" href="#六、真实项目-订单状态缓存应该怎样落地" aria-label="Permalink to “六、真实项目：订单状态缓存应该怎样落地”">​</a></h2><p>假设订单服务以 MySQL 为事实源，Redis 保存订单摘要、幂等结果和短期状态。这里不应把 Redis 持久化包装成数据库事务的一部分，而应明确恢复层级：</p><div class="language-text line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span>MySQL 订单事实</span></span>
<span class="line"><span>    ↓ 事务提交后发布变更</span></span>
<span class="line"><span>Redis 在线缓存</span></span>
<span class="line"><span>    ├─ AOF everysec：缩短节点重启后的丢失窗口</span></span>
<span class="line"><span>    ├─ RDB：时间点备份和快速恢复材料</span></span>
<span class="line"><span>    └─ 缓存修复任务：从 MySQL 重放缺失或过期数据</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>应用写路径可以采用 Cache-Aside，并给重建任务保留可观测的版本字段：</p><div class="language-java line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">@</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">Service</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">public</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> class</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> OrderCacheService</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    private</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> final</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> StringRedisTemplate redisTemplate;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    public</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> OrderCacheService</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(StringRedisTemplate </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">redisTemplate</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">        this</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">.redisTemplate </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> redisTemplate;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    }</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    public</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> void</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> refreshOrder</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(OrderSnapshot </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">order</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        String key </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;order:summary:&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> +</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> order.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">id</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">();</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">        // MySQL 已提交后再刷新缓存；Redis 失败时由重试任务修复，不能回滚数据库事实。</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        Map&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">String</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">String</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt; fields </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> Map.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">of</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">                &quot;status&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, order.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">status</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(),</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">                &quot;version&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, String.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">valueOf</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(order.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">version</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">()),</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">                &quot;updatedAt&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, order.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">updatedAt</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">().</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">toString</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">()</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        );</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        redisTemplate.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">opsForHash</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">().</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">putAll</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(key, fields);</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">        // TTL 限制陈旧缓存生命周期；具体时长应由业务读写模式决定。</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        redisTemplate.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">expire</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(key, Duration.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ofHours</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">24</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">));</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br><span class="line-number">21</span><br><span class="line-number">22</span><br><span class="line-number">23</span><br><span class="line-number">24</span><br></div></div><p>这个例子的关键不是 Java API，而是边界：AOF 减少缓存状态在进程故障后的损失，但 MySQL 仍是最终事实；恢复后要按 <code>version</code> 对账，不能只比较 Key 数量；幂等结果若会影响扣款或发货，还必须在数据库中保存可审计记录。</p><h2 id="七、故障恢复必须按剧本执行" tabindex="-1">七、故障恢复必须按剧本执行 <a class="header-anchor" href="#七、故障恢复必须按剧本执行" aria-label="Permalink to “七、故障恢复必须按剧本执行”">​</a></h2><h3 id="_1-启动前先保护现场" tabindex="-1">1. 启动前先保护现场 <a class="header-anchor" href="#_1-启动前先保护现场" aria-label="Permalink to “1. 启动前先保护现场”">​</a></h3><p>发现 AOF 截断、加载失败或磁盘满时，不要直接修改唯一文件。先停止写入或隔离故障节点，复制整个 <code>appenddirname</code>、Manifest、RDB、配置和日志，再在副本上分析。Redis 当前版本通常能够在 <code>aof-load-truncated yes</code> 时丢弃末尾不完整命令并继续加载；更严重的损坏可以使用 <code>redis-check-aof</code>，但修复可能删除无法解析之后的数据，必须先备份并审查差异。</p><h3 id="_2-同时开启-rdb-与-aof-时确认恢复来源" tabindex="-1">2. 同时开启 RDB 与 AOF 时确认恢复来源 <a class="header-anchor" href="#_2-同时开启-rdb-与-aof-时确认恢复来源" aria-label="Permalink to “2. 同时开启 RDB 与 AOF 时确认恢复来源”">​</a></h3><p>Redis 会优先用 AOF 重建数据集。若 AOF 失效而决定改用 RDB，必须把它当作一次显式降级：记录将回退到哪个时间点、预计丢失多少变更、哪些下游数据需要重放。不能为了“先启动”悄悄删除 AOF 文件。</p><h3 id="_3-恢复后验证业务不变量" tabindex="-1">3. 恢复后验证业务不变量 <a class="header-anchor" href="#_3-恢复后验证业务不变量" aria-label="Permalink to “3. 恢复后验证业务不变量”">​</a></h3><p>只验证 <code>DBSIZE</code> 不够。至少还要检查：</p><ul><li>关键业务 Key 的数量、版本和抽样内容；</li><li>过期时间是否符合预期；</li><li>Stream、消费者组、延迟任务等状态是否完整；</li><li>应用侧缓存修复和幂等逻辑是否启动；</li><li>主从角色、复制偏移和客户端路由是否正确；</li><li>恢复过程中是否发生新的写入覆盖。</li></ul><h3 id="_4-备份要做恢复演练" tabindex="-1">4. 备份要做恢复演练 <a class="header-anchor" href="#_4-备份要做恢复演练" aria-label="Permalink to “4. 备份要做恢复演练”">​</a></h3><p>RDB 文件生成成功不等于备份成功，上传对象存储成功也不等于能够恢复。应定期在隔离环境执行启动恢复，记录文件校验值、Redis 版本、加载耗时、Key 数量、业务抽样结果和实际 RPO/RTO。没有演练过的备份，只是一份尚未证实可用的文件。</p><h2 id="八、监控-不要等重启时才发现持久化坏了" tabindex="-1">八、监控：不要等重启时才发现持久化坏了 <a class="header-anchor" href="#八、监控-不要等重启时才发现持久化坏了" aria-label="Permalink to “八、监控：不要等重启时才发现持久化坏了”">​</a></h2><p><code>INFO persistence</code> 是最直接的入口。建议至少采集并告警：</p><table tabindex="0"><thead><tr><th>指标</th><th>关注点</th></tr></thead><tbody><tr><td><code>rdb_last_bgsave_status</code></td><td>最近一次后台 RDB 是否成功</td></tr><tr><td><code>rdb_last_save_time</code></td><td>距离最后成功快照是否过久</td></tr><tr><td><code>rdb_changes_since_last_save</code></td><td>未进入最近快照的变更规模</td></tr><tr><td><code>rdb_bgsave_in_progress</code></td><td>快照是否长时间未结束</td></tr><tr><td><code>aof_enabled</code></td><td>实际运行配置是否符合预期</td></tr><tr><td><code>aof_last_bgrewrite_status</code></td><td>最近一次 AOF 重写是否成功</td></tr><tr><td><code>aof_rewrite_in_progress</code> / <code>aof_rewrite_scheduled</code></td><td>重写是否卡住或反复排队</td></tr><tr><td><code>aof_current_size</code> / <code>aof_base_size</code></td><td>增长速度与重写阈值是否合理</td></tr><tr><td><code>aof_pending_bio_fsync</code> / <code>aof_delayed_fsync</code></td><td>磁盘刷写是否出现积压或延迟</td></tr><tr><td><code>latest_fork_usec</code></td><td><code>fork()</code> 对主线程暂停的影响</td></tr></tbody></table><p>还应监控磁盘剩余空间、磁盘延迟、容器内存限制、宿主机 swap、进程 RSS、复制积压和写入尾延迟。Redis 的 Latency Monitoring Framework 能记录 <code>fork</code>、<code>aof-write</code>、<code>aof-fsync-always</code>、<code>aof-rename</code> 等事件；可按业务可接受延迟设置 <code>latency-monitor-threshold</code>，再通过 <code>LATENCY LATEST</code> 或 <code>LATENCY DOCTOR</code> 排查。</p><div class="language-bash line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 查看持久化状态，不要只检查进程是否存活。</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">redis-cli</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> INFO</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> persistence</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 查看 Redis 记录的最新延迟事件。</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">redis-cli</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> LATENCY</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> LATEST</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 手动触发前先确认没有其他持久化任务和磁盘峰值。</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">redis-cli</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> BGSAVE</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">redis-cli</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> BGREWRITEAOF</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><h2 id="九、常见追问与踩坑" tabindex="-1">九、常见追问与踩坑 <a class="header-anchor" href="#九、常见追问与踩坑" aria-label="Permalink to “九、常见追问与踩坑”">​</a></h2><h3 id="_1-appendfsync-everysec-是否一定只丢一秒" tabindex="-1">1. <code>appendfsync everysec</code> 是否一定只丢一秒？ <a class="header-anchor" href="#_1-appendfsync-everysec-是否一定只丢一秒" aria-label="Permalink to “1. appendfsync everysec 是否一定只丢一秒？”">​</a></h3><p>不能把它当成跨所有故障的绝对上限。官方描述是灾难时可能丢失约一秒写入，但真实结果还受后台 <code>fsync</code> 调度、操作系统、存储设备和故障类型影响。RPO 必须用实际基础设施故障注入和恢复演练验证。</p><h3 id="_2-开启-aof-后还需要-rdb-吗" tabindex="-1">2. 开启 AOF 后还需要 RDB 吗？ <a class="header-anchor" href="#_2-开启-aof-后还需要-rdb-吗" aria-label="Permalink to “2. 开启 AOF 后还需要 RDB 吗？”">​</a></h3><p>通常仍值得保留。RDB 适合紧凑备份、异地归档和时间点回退；AOF 更侧重缩短写入丢失窗口。二者解决的问题有重叠，但并不相同。</p><h3 id="_3-aof-重写会阻塞客户端吗" tabindex="-1">3. AOF 重写会阻塞客户端吗？ <a class="header-anchor" href="#_3-aof-重写会阻塞客户端吗" aria-label="Permalink to “3. AOF 重写会阻塞客户端吗？”">​</a></h3><p>大部分重写工作由子进程完成，但 <code>fork()</code> 在主线程执行，文件切换、磁盘竞争和 COW 都可能造成延迟。正确说法是“后台重写降低了持续阻塞”，而不是“完全无阻塞”。</p><h3 id="_4-复制节点能否代替持久化" tabindex="-1">4. 复制节点能否代替持久化？ <a class="header-anchor" href="#_4-复制节点能否代替持久化" aria-label="Permalink to “4. 复制节点能否代替持久化？”">​</a></h3><p>不能。复制提升可用性，但逻辑错误可能被同步，异步复制也可能在故障切换时丢失尚未到达副本的数据。是否在副本开启持久化，要结合故障切换策略、备份职责和托管平台约束设计。</p><h3 id="_5-可以直接复制-aof-目录做备份吗" tabindex="-1">5. 可以直接复制 AOF 目录做备份吗？ <a class="header-anchor" href="#_5-可以直接复制-aof-目录做备份吗" aria-label="Permalink to “5. 可以直接复制 AOF 目录做备份吗？”">​</a></h3><p>Redis 7.0+ 的 AOF 是由 Manifest 管理的多文件集合。官方建议在复制期间避免 AOF 重写，并先确认 <code>aof_rewrite_in_progress</code> 为 0；否则可能拿到互不匹配的文件集合。备份后还要校验大小或摘要并执行恢复验证。</p><h3 id="_6-为什么磁盘满会变成线上故障" tabindex="-1">6. 为什么磁盘满会变成线上故障？ <a class="header-anchor" href="#_6-为什么磁盘满会变成线上故障" aria-label="Permalink to “6. 为什么磁盘满会变成线上故障？”">​</a></h3><p>持久化失败不仅意味着“下次重启可能丢数据”。根据配置与错误路径，Redis 还可能拒绝后续写命令；AOF 写入、重写和临时文件也都需要额外空间。磁盘水位应在耗尽前分级告警，并为重写峰值保留空间。</p><h2 id="十、选择建议与最佳实践" tabindex="-1">十、选择建议与最佳实践 <a class="header-anchor" href="#十、选择建议与最佳实践" aria-label="Permalink to “十、选择建议与最佳实践”">​</a></h2><h3 id="纯缓存-可从数据库完整重建" tabindex="-1">纯缓存，可从数据库完整重建 <a class="header-anchor" href="#纯缓存-可从数据库完整重建" aria-label="Permalink to “纯缓存，可从数据库完整重建”">​</a></h3><ul><li>可以关闭持久化，或用 RDB 缩短重启预热；</li><li>明确缓存击穿时的数据库保护和限流方案；</li><li>恢复目标是业务容量，不是恢复每一条缓存记录。</li></ul><h3 id="可容忍秒级丢失的业务状态" tabindex="-1">可容忍秒级丢失的业务状态 <a class="header-anchor" href="#可容忍秒级丢失的业务状态" aria-label="Permalink to “可容忍秒级丢失的业务状态”">​</a></h3><ul><li>常见选择是 AOF <code>everysec</code>，并保留周期性 RDB；</li><li>通过副本、异机备份和业务重放进一步缩小影响；</li><li>对 <code>aof_delayed_fsync</code>、重写失败和磁盘延迟设置告警。</li></ul><h3 id="不能容忍数据丢失的核心账务" tabindex="-1">不能容忍数据丢失的核心账务 <a class="header-anchor" href="#不能容忍数据丢失的核心账务" aria-label="Permalink to “不能容忍数据丢失的核心账务”">​</a></h3><ul><li>不要仅依赖 Redis <code>appendfsync always</code>；</li><li>使用具备明确事务和复制承诺的事实存储；</li><li>Redis 保存可重建视图或加速数据，写路径保留幂等日志与对账能力。</li></ul><h3 id="通用落地清单" tabindex="-1">通用落地清单 <a class="header-anchor" href="#通用落地清单" aria-label="Permalink to “通用落地清单”">​</a></h3><ol><li>用 RPO、RTO 和故障域选择策略，不照抄默认配置；</li><li>标注 Redis 7.0+ 多文件 AOF 的目录、Manifest 和备份流程；</li><li>给 <code>fork()</code>、COW、重写临时文件和复制缓冲留出 CPU、内存与磁盘余量；</li><li>拆分大 Key，同时控制 Key 数量和原子操作边界；</li><li>错开 RDB、AOF 重写、宿主机快照和批处理 I/O 峰值；</li><li>监控持久化结果、延迟、磁盘空间，而不是只监控 Redis 存活；</li><li>修改运行配置后同步持久化到配置文件，避免重启回退；</li><li>备份文件、配置、版本信息和校验值，并跨机器或跨故障域保存；</li><li>在隔离环境定期恢复，验证业务不变量和真实 RPO/RTO；</li><li>把修复、降级和数据回灌写成演练过的操作手册。</li></ol><h2 id="总结" tabindex="-1">总结 <a class="header-anchor" href="#总结" aria-label="Permalink to “总结”">​</a></h2><p>Redis 持久化的主线可以压缩成一句话：RDB 保存时间点，AOF 保存写入轨迹，重写把不断增长的历史重新折叠为可恢复的当前状态。</p><p>真正困难的地方不在于写下 <code>appendonly yes</code>，而在于把数据安全、请求延迟、内存余量、磁盘空间和恢复时间放进同一套约束。RDB 的 <code>fork()</code> 与 COW、AOF 的三种 <code>fsync</code> 策略、Redis 7.0+ 的 Base/Incremental/Manifest、多文件备份边界以及大 Key 放大效应，都必须进入容量规划和监控。</p><p>最后记住：持久化文件只有经过异地保存和恢复演练，才是可信的恢复材料；Redis 只有与事实源、复制、备份、对账和故障剧本一起设计，才是一套完整的数据安全方案。</p><h2 id="参考资料" tabindex="-1">参考资料 <a class="header-anchor" href="#参考资料" aria-label="Permalink to “参考资料”">​</a></h2><ul><li><a href="https://redis.io/docs/latest/operate/oss_and_stack/management/persistence/" target="_blank" rel="noreferrer">Redis 官方文档：Persistence</a></li><li><a href="https://redis.io/docs/latest/commands/info/" target="_blank" rel="noreferrer">Redis 官方文档：INFO</a></li><li><a href="https://redis.io/docs/latest/operate/oss_and_stack/management/optimization/latency/" target="_blank" rel="noreferrer">Redis 官方文档：Diagnosing latency issues</a></li><li><a href="https://redis.io/docs/latest/operate/oss_and_stack/management/optimization/latency-monitor/" target="_blank" rel="noreferrer">Redis 官方文档：Latency monitoring</a></li><li><a href="https://redis.io/docs/latest/commands/" target="_blank" rel="noreferrer">Redis 官方文档：Redis 8.8 Commands Reference</a></li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("posts/redis-persistence-rdb-aof-engineering.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const redisPersistenceRdbAofEngineering = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  redisPersistenceRdbAofEngineering as default
};
