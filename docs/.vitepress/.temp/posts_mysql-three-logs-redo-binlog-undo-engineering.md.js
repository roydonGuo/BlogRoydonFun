import { ssrRenderAttrs, ssrRenderAttr, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const _imports_0 = "/images/posts/mysql-three-logs-redo-binlog-undo-engineering-knowledge-map.png";
const __pageData = JSON.parse('{"title":"MySQL 三大日志工程实践：redo log、binlog 与 undo log 的分工与协作","description":"","frontmatter":{"title":"MySQL 三大日志工程实践：redo log、binlog 与 undo log 的分工与协作","date":"2026-08-19T00:00:00.000Z","category":"MySQL","cover":"/images/posts/mysql-three-logs-redo-binlog-undo-engineering-knowledge-map.png","tags":["mysql","innodb","redo-log","binlog","undo-log","wal"],"excerpt":"一次 UPDATE 会同时留下三条日志：redo log 负责崩溃后重放已提交数据，binlog 负责把逻辑变更复制到从库，undo log 负责回滚与 MVCC 旧版本。本文讲清三者分工、两阶段提交、刷盘参数与常见踩坑。"},"headers":[],"relativePath":"posts/mysql-three-logs-redo-binlog-undo-engineering.md","filePath":"posts/mysql-three-logs-redo-binlog-undo-engineering.md","lastUpdated":1787128073000}');
const _sfc_main = { name: "posts/mysql-three-logs-redo-binlog-undo-engineering.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="mysql-三大日志工程实践-redo-log、binlog-与-undo-log-的分工与协作" tabindex="-1">MySQL 三大日志工程实践：redo log、binlog 与 undo log 的分工与协作 <a class="header-anchor" href="#mysql-三大日志工程实践-redo-log、binlog-与-undo-log-的分工与协作" aria-label="Permalink to “MySQL 三大日志工程实践：redo log、binlog 与 undo log 的分工与协作”">​</a></h1><img${ssrRenderAttr("src", _imports_0)} alt="MySQL 三大日志工程实践：redo log、binlog 与 undo log 的分工与协作知识串联图" style="${ssrRenderStyle({ "border-radius": "10px" })}"><p>一次 UPDATE 会同时留下三条日志：redo log 负责崩溃后重放已提交数据，binlog 负责把逻辑变更复制到从库，undo log 负责回滚与 MVCC 旧版本。本文讲清三者分工、两阶段提交、刷盘参数与常见踩坑。</p><h2 id="先说结论-一次-update-三条日志各管一摊" tabindex="-1">先说结论：一次 UPDATE，三条日志各管一摊 <a class="header-anchor" href="#先说结论-一次-update-三条日志各管一摊" aria-label="Permalink to “先说结论：一次 UPDATE，三条日志各管一摊”">​</a></h2><p>很多人背过&quot;redo 管崩溃恢复、binlog 管主从、undo 管回滚&quot;，但遇到&quot;为什么提交时要先 prepare 再 commit&quot;&quot;undo 表空间为什么暴涨&quot;&quot;双 1 到底防什么&quot;这类问题就说不清了。本文把这些串起来。</p><p>一条 <code>UPDATE</code> 在执行与提交过程中，会同时产生三条日志记录，各自服务于完全不同的目标：</p><ol><li><strong>undo log 最先写</strong>：记录&quot;这行数据改之前长什么样&quot;，事务回滚和 MVCC 旧版本读取都靠它；</li><li><strong>redo log</strong>：记录&quot;哪个数据页被改成了什么&quot;，实例崩溃后按它重放，保证<strong>已提交</strong>的数据不丢（持久性）；</li><li><strong>binlog</strong>：Server 层逻辑日志，记录&quot;这次执行了什么变更&quot;，主从复制、时间点恢复和审计靠它。</li></ol><p>先记住三条工程结论：</p><ol><li><strong>持久性靠 redo，不靠立刻刷数据页</strong>。WAL（Write-Ahead Logging）先写日志、后刷脏页，把随机 IO 变成顺序 IO；</li><li><strong>主从复制靠 binlog</strong>。redo log 是存储引擎层的物理日志，只在本实例内部生效，无法跨实例共享；</li><li><strong>redo 与 binlog 通过两阶段提交保持一致</strong>。崩溃恢复时，处于 PREPARE 状态的事务以&quot;binlog 是否完整&quot;为裁决依据，决定提交还是回滚。</li></ol><p>本文以 MySQL 8.4 LTS、InnoDB 为基线，事实核对时间为 2026-08-19。redo log 容量机制、undo 表空间等涉及 8.0.30+ 的行为差异均已按官方文档标注。主要参考：<a href="https://dev.mysql.com/doc/refman/8.4/en/innodb-redo-log.html" target="_blank" rel="noreferrer">Redo Log 官方文档</a>、<a href="https://dev.mysql.com/doc/refman/8.4/en/binary-log.html" target="_blank" rel="noreferrer">Binary Log 官方文档</a>、<a href="https://dev.mysql.com/doc/refman/8.4/en/innodb-undo-logs.html" target="_blank" rel="noreferrer">Undo Logs 官方文档</a>、<a href="https://dev.mysql.com/doc/refman/8.4/en/innodb-flush-log-at-trx-commit.html" target="_blank" rel="noreferrer">InnoDB 刷盘参数</a>。</p><h2 id="一、三大日志全景-一张表分清职责" tabindex="-1">一、三大日志全景：一张表分清职责 <a class="header-anchor" href="#一、三大日志全景-一张表分清职责" aria-label="Permalink to “一、三大日志全景：一张表分清职责”">​</a></h2><p>把三条日志放进同一张表，差异一目了然：</p><table tabindex="0"><thead><tr><th>日志</th><th>所在层</th><th>记录内容</th><th>类型</th><th>核心用途</th><th>写入与清理方式</th></tr></thead><tbody><tr><td>redo log</td><td>InnoDB 存储引擎层</td><td>数据页的物理变更（页号、偏移、新值）</td><td>物理日志</td><td>崩溃恢复、持久性</td><td>循环写，容量由 <code>innodb_redo_log_capacity</code> 控制，8.0.30+ 默认 100MB</td></tr><tr><td>binlog</td><td>Server 层</td><td>执行的逻辑变更（语句或行前后像）</td><td>逻辑日志</td><td>主从复制、时间点恢复、审计</td><td>追加写，按 <code>max_binlog_size</code> 滚动新文件，按 <code>binlog_expire_logs_seconds</code> 过期清理</td></tr><tr><td>undo log</td><td>InnoDB 存储引擎层</td><td>事务修改前的旧版本数据（反操作）</td><td>逻辑反操作</td><td>事务回滚、MVCC 版本链</td><td>存放在独立 undo 表空间（默认 <code>undo_001</code>/<code>undo_002</code>），由 purge 线程按需清理</td></tr></tbody></table><p>要点：<strong>redo 是&quot;物理&quot;的，binlog 是&quot;逻辑&quot;的，undo 是&quot;反操作&quot;的</strong>。这三句话能解释绝大多数面试追问。</p><p>另外还有几类日志，别和它们混为一谈：</p><ul><li><strong>error log</strong>：错误日志，排查启动失败和崩溃原因；</li><li><strong>slow query log</strong>：慢查询日志，DBA 调优入口；</li><li><strong>relay log</strong>：从库专属，是 binlog 在从库的中转站；</li><li><strong>general log</strong>：记录所有请求的全量日志，性能开销大，一般不开启。</li></ul><h2 id="二、redo-log-崩溃后把已提交的数据-重做-回来" tabindex="-1">二、redo log：崩溃后把已提交的数据&quot;重做&quot;回来 <a class="header-anchor" href="#二、redo-log-崩溃后把已提交的数据-重做-回来" aria-label="Permalink to “二、redo log：崩溃后把已提交的数据&quot;重做&quot;回来”">​</a></h2><h3 id="为什么必须有它" tabindex="-1">为什么必须有它 <a class="header-anchor" href="#为什么必须有它" aria-label="Permalink to “为什么必须有它”">​</a></h3><p>InnoDB 用 Buffer Pool 缓存数据页，读写都在内存里进行，性能好，但<strong>内存不可靠</strong>——断电或进程崩溃，脏页（已修改未刷盘的数据页）会丢。如果每次提交都同步把数据页刷到磁盘，写入性能会被随机 IO 拖垮。</p><p>WAL 的思路是：<strong>先写日志，再刷数据</strong>。更新发生时，先把&quot;这次对哪个页做了什么修改&quot;以顺序追加的方式写进 redo log，数据页留在内存里，后台再择机刷盘。这样即使崩溃，也能靠 redo log 把修改重放回来。</p><h3 id="一条-update-时-redo-怎么流转" tabindex="-1">一条 UPDATE 时 redo 怎么流转 <a class="header-anchor" href="#一条-update-时-redo-怎么流转" aria-label="Permalink to “一条 UPDATE 时 redo 怎么流转”">​</a></h3><ol><li>在 Buffer Pool 中修改目标数据页，标记为脏页；</li><li>生成对应的 redo 记录，写入内存中的 <strong>redo log buffer</strong>；</li><li>事务提交时，按 <code>innodb_flush_log_at_trx_commit</code> 决定 redo 何时刷盘；</li><li>后台线程在合适时机把脏页刷到磁盘，并推进 checkpoint。</li></ol><h3 id="刷盘参数-0-1-2" tabindex="-1">刷盘参数：0 / 1 / 2 <a class="header-anchor" href="#刷盘参数-0-1-2" aria-label="Permalink to “刷盘参数：0 / 1 / 2”">​</a></h3><table tabindex="0"><thead><tr><th>取值</th><th>行为</th><th>安全性</th><th>适用场景</th></tr></thead><tbody><tr><td>0</td><td>每秒由后台线程刷一次盘</td><td>实例崩溃最多丢约 1 秒已提交事务</td><td>对数据丢失容忍度高、追求极致写性能</td></tr><tr><td>1</td><td>每次事务提交都 fsync 到磁盘</td><td>最安全，已提交事务不丢</td><td><strong>默认值</strong>，生产首选</td></tr><tr><td>2</td><td>提交时只写入操作系统缓存，每秒刷盘</td><td>mysqld 崩溃不丢，但 OS 宕机/断电可能丢约 1 秒</td><td>可接受操作系统级故障丢失</td></tr></tbody></table><h3 id="容量与循环写-8-0-30-之后变化较大" tabindex="-1">容量与循环写（8.0.30 之后变化较大） <a class="header-anchor" href="#容量与循环写-8-0-30-之后变化较大" aria-label="Permalink to “容量与循环写（8.0.30 之后变化较大）”">​</a></h3><p>redo log 是<strong>循环写</strong>的：写满后从最早的位置覆盖，前提是 checkpoint 已经推进，说明对应的脏页已安全刷盘。</p><ul><li><strong>8.0.30 之前</strong>：默认两个文件 <code>ib_logfile0</code>、<code>ib_logfile1</code>，由 <code>innodb_log_file_size</code> 和 <code>innodb_log_files_in_group</code> 控制；</li><li><strong>8.0.30 起</strong>：改用 <code>innodb_redo_log_capacity</code> 统一控制总容量，默认 <strong>100MB</strong>（最大 128GB），文件放在数据目录下的 <code>#innodb_redo/</code> 中（约 32 个 <code>#ib_redo*</code> 文件），并支持<strong>运行时动态调整</strong>。</li></ul><div class="language-sql line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- 查看当前 redo 容量配置</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">SHOW VARIABLES </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">LIKE</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;innodb_redo_log_capacity&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- 运行时调大（如批量导入大事务前临时扩容）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">SET</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> GLOBAL</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> innodb_redo_log_capacity </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 4</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> *</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 1024</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> *</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 1024</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> *</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 1024</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>注意：如果 redo log 写满但 checkpoint 迟迟追不上（通常因为脏页刷盘太慢），InnoDB 会<strong>强制刷脏页</strong>，表现为写入骤停、性能断崖。批量大事务场景下容量给得太小，这是最常见的翻车点。</p><h3 id="崩溃恢复-从-checkpoint-向后重放" tabindex="-1">崩溃恢复：从 checkpoint 向后重放 <a class="header-anchor" href="#崩溃恢复-从-checkpoint-向后重放" aria-label="Permalink to “崩溃恢复：从 checkpoint 向后重放”">​</a></h3><p>实例启动时，InnoDB 从最近一次 checkpoint 记录的 LSN 位置开始，向后重放 redo log，把崩溃前已提交的事务恢复出来。处于 PREPARE 状态（提交到一半）的事务，则要看 binlog 是否完整——这就是下一节要讲的两阶段提交。</p><p>监控方式：<code>SHOW ENGINE INNODB STATUS</code> 的 LOG 段，或查 <code>information_schema.innodb_metrics</code> 中 <code>log_lsn_*</code> 系列指标，观察 redo 写入与 checkpoint 之间的差距。</p><h2 id="三、binlog-把-逻辑变更-复制到从库、恢复现场" tabindex="-1">三、binlog：把&quot;逻辑变更&quot;复制到从库、恢复现场 <a class="header-anchor" href="#三、binlog-把-逻辑变更-复制到从库、恢复现场" aria-label="Permalink to “三、binlog：把&quot;逻辑变更&quot;复制到从库、恢复现场”">​</a></h2><h3 id="定位" tabindex="-1">定位 <a class="header-anchor" href="#定位" aria-label="Permalink to “定位”">​</a></h3><p>binlog 是 <strong>Server 层</strong>的逻辑日志，和存储引擎无关——用任何引擎（包括 MyISAM）都会记录。它记录的是&quot;这次执行了什么变更&quot;，而不是&quot;哪个页被改了&quot;。</p><h3 id="三种格式" tabindex="-1">三种格式 <a class="header-anchor" href="#三种格式" aria-label="Permalink to “三种格式”">​</a></h3><table tabindex="0"><thead><tr><th>格式</th><th>记录内容</th><th>优点</th><th>缺点</th></tr></thead><tbody><tr><td>STATEMENT</td><td>原始 SQL 语句</td><td>体积小</td><td>非确定函数（<code>NOW()</code>、<code>UUID()</code>）、自增等场景可能导致主从不一致</td></tr><tr><td>ROW</td><td>每一行变更的前后像</td><td>最安全，复制结果精确一致</td><td>体积大</td></tr><tr><td>MIXED</td><td>自动切换</td><td>默认 STATEMENT，检测到不安全语句自动转 ROW</td><td>行为有隐式切换，排障时略绕</td></tr></tbody></table><p>MySQL 8.0 起默认就是 <strong>ROW</strong>。8.4 中基于 writeset 的冲突检测也仅支持 ROW 格式，所以生产环境就选 ROW，不必犹豫。</p><h3 id="写入与刷盘" tabindex="-1">写入与刷盘 <a class="header-anchor" href="#写入与刷盘" aria-label="Permalink to “写入与刷盘”">​</a></h3><p>事务内的 binlog 先写进内存的 <strong>binlog cache</strong>（<code>binlog_cache_size</code>），提交时刷到 binlog 文件。刷盘频率由 <code>sync_binlog</code> 控制：</p><ul><li><code>0</code>：交给操作系统决定何时落盘，性能最好但可能丢 binlog；</li><li><code>1</code>：<strong>每次提交都 fsync</strong>（默认值），最安全；</li><li><code>N</code>：每 N 次提交刷一次盘。</li></ul><p>生产环境的&quot;<strong>双 1</strong>&quot;配置，就是 <code>innodb_flush_log_at_trx_commit=1</code> + <code>sync_binlog=1</code>：redo 和 binlog 每次提交都落盘，正常情况下已提交事务不丢。</p><h3 id="三大用途" tabindex="-1">三大用途 <a class="header-anchor" href="#三大用途" aria-label="Permalink to “三大用途”">​</a></h3><ol><li><strong>主从复制</strong>：主库的 dump 线程把 binlog 推给从库，从库 IO 线程写入 relay log，SQL 线程再重放；</li><li><strong>时间点恢复</strong>：全量备份 + 回放 binlog 到指定 GTID 或位置，把误删前的数据找回来；</li><li><strong>审计与订正</strong>：从 binlog 反查某段时间内谁改了哪些数据。</li></ol><h3 id="文件管理" tabindex="-1">文件管理 <a class="header-anchor" href="#文件管理" aria-label="Permalink to “文件管理”">​</a></h3><ul><li><code>max_binlog_size</code> 默认 1GB，写满自动滚动新文件；</li><li><code>binlog_expire_logs_seconds</code> 控制过期清理（默认 30 天；8.4 已移除旧的 <code>expire_logs_days</code>）；</li><li>8.4 起复制相关语法全面改为 <code>SOURCE</code>/<code>REPLICA</code> 命名（如 <code>CHANGE REPLICATION SOURCE TO</code>），GTID 复制是主流做法。</li></ul><h2 id="四、undo-log-回滚的底气-也是-mvcc-的时光机" tabindex="-1">四、undo log：回滚的底气，也是 MVCC 的时光机 <a class="header-anchor" href="#四、undo-log-回滚的底气-也是-mvcc-的时光机" aria-label="Permalink to “四、undo log：回滚的底气，也是 MVCC 的时光机”">​</a></h2><h3 id="两种记录" tabindex="-1">两种记录 <a class="header-anchor" href="#两种记录" aria-label="Permalink to “两种记录”">​</a></h3><ul><li><strong>insert undo</strong>：INSERT 产生，只用于回滚（撤销插入），purge 线程可以快速回收；</li><li><strong>update undo</strong>：UPDATE/DELETE 产生，除了回滚，还要承担 MVCC 旧版本读取，生命周期更长，也是 undo 膨胀的主要来源。</li></ul><h3 id="两个核心作用" tabindex="-1">两个核心作用 <a class="header-anchor" href="#两个核心作用" aria-label="Permalink to “两个核心作用”">​</a></h3><ol><li><strong>事务回滚</strong>：<code>ROLLBACK</code> 时按 undo log 反向执行，把数据恢复成修改前的样子，保证原子性；</li><li><strong>MVCC 版本链</strong>：聚簇索引记录上有隐藏列 <code>roll_pointer</code>，指向 undo log 中的旧版本记录，形成版本链。Read View 顺着版本链找到对当前事务可见的版本，实现无锁的一致性读。</li></ol><h3 id="存储与清理" tabindex="-1">存储与清理 <a class="header-anchor" href="#存储与清理" aria-label="Permalink to “存储与清理”">​</a></h3><ul><li>MySQL 8.0 起，undo log 从系统表空间（ibdata）中独立出来，存放在专门的 undo 表空间：默认 <code>undo_001</code>、<code>undo_002</code>（<code>.ibu</code> 文件），<strong>至少保持 2 个</strong>才能支持自动截断；</li><li>8.0.14 起可以用 SQL 在线管理：<code>CREATE UNDO TABLESPACE</code> / <code>ALTER UNDO TABLESPACE ... SET INACTIVE</code> / <code>DROP UNDO TABLESPACE</code>（最多 127 个）；</li><li>清理由 <strong>purge 线程</strong>完成：<code>innodb_undo_log_truncate</code> 默认 ON，undo 表空间超过 <code>innodb_max_undo_log_size</code>（默认 1GB）时会自动截断回收。</li></ul><h3 id="风险与监控" tabindex="-1">风险与监控 <a class="header-anchor" href="#风险与监控" aria-label="Permalink to “风险与监控”">​</a></h3><p>undo 表空间膨胀、查询变慢，十有八九是<strong>长事务或长查询</strong>：只要还有事务引用旧版本，对应 undo 就不能被 purge。</p><ul><li>看 <code>SHOW ENGINE INNODB STATUS</code> 里的 <strong>History List Length（HLL）</strong>，持续走高说明 purge 追不上；</li><li>用 <code>information_schema.innodb_trx</code> 找运行最久的事务，评估后处理；</li><li><strong>严禁手动删除 <code>undo_*.ibu</code> 文件</strong>，那不是释放空间，是制造灾难。</li></ul><h2 id="五、协作-一条-update-的完整旅程与两阶段提交" tabindex="-1">五、协作：一条 UPDATE 的完整旅程与两阶段提交 <a class="header-anchor" href="#五、协作-一条-update-的完整旅程与两阶段提交" aria-label="Permalink to “五、协作：一条 UPDATE 的完整旅程与两阶段提交”">​</a></h2><p>把三条日志串起来看一条 <code>UPDATE</code>：</p><div class="language-text line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span>1. BEGIN，事务分配事务 id</span></span>
<span class="line"><span>2. 加锁：MDL（表结构）+ 行锁</span></span>
<span class="line"><span>3. 写 undo log：记录旧值，roll_pointer 指向旧版本   ← 旧账</span></span>
<span class="line"><span>4. 更新 Buffer Pool 中的数据页，标记为脏页</span></span>
<span class="line"><span>5. 写 redo log buffer                                 ← 重做账（先写内存）</span></span>
<span class="line"><span>6. 提交：两阶段提交</span></span>
<span class="line"><span>   ├─ 阶段一：redo prepare（按 innodb_flush_log_at_trx_commit 刷盘）</span></span>
<span class="line"><span>   ├─ 阶段二：binlog write + fsync（按 sync_binlog）   ← 复制账</span></span>
<span class="line"><span>   └─ 阶段三：redo commit（打上 commit 标记）</span></span>
<span class="line"><span>7. 后台：脏页异步刷盘，checkpoint 推进，redo 可被覆盖</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><h3 id="为什么需要两阶段提交" tabindex="-1">为什么需要两阶段提交 <a class="header-anchor" href="#为什么需要两阶段提交" aria-label="Permalink to “为什么需要两阶段提交”">​</a></h3><p>redo log 和 binlog 分属两层（引擎层 / Server 层），各自独立落盘。如果不做协调，崩溃时可能只写了其中一个：</p><ul><li>只有 redo 没有 binlog → 主库恢复了，但从库没收到这条变更，主从不一致；</li><li>只有 binlog 没有 redo → 从库多执行了，主库却没有，同样不一致。</li></ul><p>所以两阶段提交以 <strong>binlog 是否完整</strong> 作为 PREPARE 状态事务的裁决依据：binlog 完整 → 提交；不完整 → 用 undo 回滚。这保证了&quot;主库看到的提交集合&quot;与&quot;从库复制的提交集合&quot;严格一致。</p><h3 id="组提交-group-commit" tabindex="-1">组提交（group commit） <a class="header-anchor" href="#组提交-group-commit" aria-label="Permalink to “组提交（group commit）”">​</a></h3><p>高并发下如果每个事务提交都做一次 fsync，代价很高。InnoDB 会把多个事务的 prepare/binlog/commit 阶段<strong>批量合并</strong>刷盘（<code>binlog_group_commit_sync_delay</code>、<code>binlog_group_commit_sync_no_delay_count</code> 可调），这就是组提交。它解释了&quot;为什么双 1 不一定拖垮性能&quot;。</p><h2 id="六、真实场景-订单状态更新、主从复制与误删恢复" tabindex="-1">六、真实场景：订单状态更新、主从复制与误删恢复 <a class="header-anchor" href="#六、真实场景-订单状态更新、主从复制与误删恢复" aria-label="Permalink to “六、真实场景：订单状态更新、主从复制与误删恢复”">​</a></h2><h3 id="场景一-订单支付回调" tabindex="-1">场景一：订单支付回调 <a class="header-anchor" href="#场景一-订单支付回调" aria-label="Permalink to “场景一：订单支付回调”">​</a></h3><div class="language-sql line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">-- 订单支付成功回调</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">UPDATE</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> orders </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">SET</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> status</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;PAID&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, paid_at </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> NOW</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">()</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">WHERE</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> order_no </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;202608191001&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>这条语句执行后：</p><table tabindex="0"><thead><tr><th>日志</th><th>它记录了什么</th></tr></thead><tbody><tr><td>undo log</td><td>该行原来的 <code>status=&#39;UNPAID&#39;</code> 旧值（回滚 + MVCC 旧版本）</td></tr><tr><td>redo log</td><td><code>orders</code> 表对应数据页的物理变更（页号 + 新值），供崩溃重放</td></tr><tr><td>binlog（ROW）</td><td><code>orders</code> 表这一行的变更前后像，供从库复制、审计、闪回</td></tr></tbody></table><h3 id="场景二-主从复制链路" tabindex="-1">场景二：主从复制链路 <a class="header-anchor" href="#场景二-主从复制链路" aria-label="Permalink to “场景二：主从复制链路”">​</a></h3><div class="language-text line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span>主库 binlog ──dump 线程推送──▶ 从库 IO 线程 ──写入──▶ relay log ──SQL 线程重放──▶ 从库数据</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>排查从库延迟时，看 <code>SHOW REPLICA STATUS</code> 的 <code>Seconds_Behind_Source</code>（8.4 命名）。从库延迟通常是：大事务在主库很快、在从库重放慢，或从库查询压力大。ROWS 格式下大事务会产生海量 binlog，这是延迟最常见的来源。</p><h3 id="场景三-误删恢复" tabindex="-1">场景三：误删恢复 <a class="header-anchor" href="#场景三-误删恢复" aria-label="Permalink to “场景三：误删恢复”">​</a></h3><p>误删一张表后，标准流程是：</p><ol><li>找最近一次<strong>全量备份</strong>（逻辑备份 mysqldump 或物理备份）；</li><li>用 <code>mysqlbinlog</code> 把误删时间点之前的 binlog 导出来回放，恢复到误删前一刻；</li><li>验证数据后再切流量。</li></ol><p>所以&quot;备份&quot;从来不只是全量备份，<strong>binlog 就是你的增量备份</strong>。<code>binlog_expire_logs_seconds</code> 要覆盖你&quot;从发现到恢复&quot;的最长窗口。</p><h2 id="七、常见追问" tabindex="-1">七、常见追问 <a class="header-anchor" href="#七、常见追问" aria-label="Permalink to “七、常见追问”">​</a></h2><p><strong>redo log 和 binlog 有什么区别？</strong> 物理 vs 逻辑；InnoDB 层 vs Server 层；循环写 vs 追加写；作用上 redo 管崩溃恢复（本实例），binlog 管复制/恢复/审计（跨实例）。redo 记录&quot;页变了&quot;，binlog 记录&quot;变更发生了&quot;。</p><p><strong>为什么有了 redo 还需要 binlog？</strong> redo 是引擎内部物理日志，无法跨实例共享；复制、时间点恢复、审计都需要 Server 层的逻辑日志。两者不是替代关系，是分工关系。</p><p><strong><code>innodb_flush_log_at_trx_commit=2</code> 安全吗？</strong> 对 mysqld 崩溃安全（数据已进 OS 缓存），但对 OS 宕机、断电不安全，可能丢约 1 秒已提交事务。选 0/1/2 本质是&quot;性能 vs 丢多少数据&quot;的权衡。</p><p><strong>双 1 就一定不丢数据吗？</strong> 正常磁盘故障下，已提交事务不丢；但磁盘物理损坏、文件系统异常等仍可能丢，所以备份和主从依然必不可少。</p><p><strong>binlog 三种格式怎么选？</strong> 生产用 ROW（8.0 默认、8.4 推荐）；老版本兼容或体积敏感可评估 MIXED；STATEMENT 不要用于生产复制。</p><p><strong>undo 表空间暴涨怎么办？</strong> 先查长事务：<code>SELECT * FROM information_schema.innodb_trx ORDER BY trx_started LIMIT 10;</code>，评估后 kill 或等其结束，purge 会自动回收；确认 <code>innodb_undo_log_truncate=ON</code>。不要手动删文件。</p><p><strong>redo log 写满会怎样？</strong> checkpoint 追不上时 InnoDB 会强制刷脏页，写入卡顿。大事务批量写入前评估容量，或临时调大 <code>innodb_redo_log_capacity</code>。</p><p><strong>从库读的也是 binlog 吗？</strong> 从库读的是 relay log——binlog 在从库的中转文件，SQL 线程从 relay log 重放。复制延迟排查主要看 relay log 与 SQL 线程状态。</p><h2 id="八、踩坑与最佳实践" tabindex="-1">八、踩坑与最佳实践 <a class="header-anchor" href="#八、踩坑与最佳实践" aria-label="Permalink to “八、踩坑与最佳实践”">​</a></h2><ol><li><strong>双 1 是底线</strong>：<code>innodb_flush_log_at_trx_commit=1</code> + <code>sync_binlog=1</code>，配 SSD；实在要性能再说服自己降级并明确丢失窗口；</li><li><strong>binlog 用 ROW</strong>，过期时间按备份窗口设置（如 7~30 天），别让 binlog 占满磁盘或过早过期；</li><li><strong>绝不手动删除日志文件</strong>：redo、undo、binlog 都是数据库自身管理，删文件等于自杀；</li><li><strong>大事务拆批</strong>：一批 10 万行的 UPDATE 会让 redo 写满、从库延迟、undo 暴涨，改成小批循环提交；</li><li><strong>长事务治理</strong>：监控 <code>innodb_trx</code> 与 History List Length，定时任务里开事务记得及时提交；</li><li><strong>备份 = 全量 + binlog 增量</strong>，并且要定期演练恢复，别等出事了第一次跑恢复流程；</li><li><strong>留意 8.4 默认值变化</strong>：<code>innodb_log_buffer_size</code> 默认从 16MiB 提到 64MiB、Linux 下 <code>innodb_flush_method</code> 默认 <code>O_DIRECT</code>、<code>innodb_io_capacity</code> 默认 10000，升级后行为可能与旧版本不同，先在测试环境压测。</li></ol><h2 id="九、总结" tabindex="-1">九、总结 <a class="header-anchor" href="#九、总结" aria-label="Permalink to “九、总结”">​</a></h2><p>一条 UPDATE 的本质是&quot;三方对账&quot;：先写<strong>旧账</strong>（undo log），再记<strong>重做账</strong>（redo log），提交时发<strong>复制账</strong>（binlog），并通过两阶段提交保证重做账与复制账一致。</p><ul><li>崩溃了，靠 redo 重放已提交数据；</li><li>要复制，靠 binlog 把变更推给从库；</li><li>要反悔，靠 undo 回滚、靠版本链读旧值。</li></ul><p>把这三个&quot;账本&quot;的写入时机和清理机制想清楚，MySQL 的持久性、复制和高可用问题就都有了解题的锚点。</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("posts/mysql-three-logs-redo-binlog-undo-engineering.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const mysqlThreeLogsRedoBinlogUndoEngineering = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  mysqlThreeLogsRedoBinlogUndoEngineering as default
};
