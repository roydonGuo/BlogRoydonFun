import { ssrRenderAttrs, ssrRenderAttr, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const _imports_0 = "/images/posts/jvm-gc-garbage-collection-engineering-knowledge-map.png";
const __pageData = JSON.parse('{"title":"JVM 垃圾回收工程实践：可达性分析、三色标记、分代收集与收集器选型","description":"","frontmatter":{"title":"JVM 垃圾回收工程实践：可达性分析、三色标记、分代收集与收集器选型","date":"2026-08-19T00:00:00.000Z","category":"后端开发","cover":"/images/posts/jvm-gc-garbage-collection-engineering-knowledge-map.png","tags":["jvm","gc","垃圾回收","可达性分析","三色标记","g1","zgc","分代收集"],"excerpt":"GC 管的不是整个 JVM，而是堆里\\"能不能被根引用链摸到\\"的对象。本文从判定对象死活的两条路（引用计数 / 可达性分析 / 三色标记）讲起，串起四种引用、三种回收算法、分代模型，再到 Serial / Parallel / CMS / G1 / ZGC 的取舍，并给出 Spring Boot 调优、GC 日志、OOM 排查与选型建议。"},"headers":[],"relativePath":"posts/jvm-gc-garbage-collection-engineering.md","filePath":"posts/jvm-gc-garbage-collection-engineering.md","lastUpdated":1787131359000}');
const _sfc_main = { name: "posts/jvm-gc-garbage-collection-engineering.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="jvm-垃圾回收工程实践-可达性分析、三色标记、分代收集与收集器选型" tabindex="-1">JVM 垃圾回收工程实践：可达性分析、三色标记、分代收集与收集器选型 <a class="header-anchor" href="#jvm-垃圾回收工程实践-可达性分析、三色标记、分代收集与收集器选型" aria-label="Permalink to “JVM 垃圾回收工程实践：可达性分析、三色标记、分代收集与收集器选型”">​</a></h1><img${ssrRenderAttr("src", _imports_0)} alt="JVM 垃圾回收工程实践：可达性分析、三色标记、分代收集与收集器选型知识串联图" style="${ssrRenderStyle({ "border-radius": "10px" })}"><p>GC 管的不是整个 JVM，而是堆里&quot;能不能被根引用链摸到&quot;的对象。本文从判定对象死活的两条路（引用计数 / 可达性分析 / 三色标记）讲起，串起四种引用、三种回收算法、分代模型，再到 Serial / Parallel / CMS / G1 / ZGC 的取舍，并给出 Spring Boot 调优、GC 日志、OOM 排查与选型建议。</p><h2 id="先说结论-gc-只解决-堆里哪些对象该回收" tabindex="-1">先说结论：GC 只解决&quot;堆里哪些对象该回收&quot; <a class="header-anchor" href="#先说结论-gc-只解决-堆里哪些对象该回收" aria-label="Permalink to “先说结论：GC 只解决&quot;堆里哪些对象该回收&quot;”">​</a></h2><p>很多人把 GC 想成&quot;自动管内存的魔法&quot;，但落到工程上真正要回答的只有三个问题：</p><ol><li><strong>哪些对象算&quot;死&quot;了？</strong> —— 不被 GC Roots 引用链触及的对象，即判定为可回收。</li><li><strong>怎么回收才高效？</strong> —— 标记-清除 / 复制 / 标记-整理三选一，本质是在&quot;碎片、内存占用、停顿&quot;之间做权衡。</li><li><strong>谁来回收、何时停顿？</strong> —— 不同收集器在&quot;吞吐优先&quot;和&quot;停顿优先&quot;之间取舍；JDK 9 起 <strong>G1 是默认收集器</strong>，JDK 15 起 ZGC / Shenandoah 已可用于生产但仍非默认。</li></ol><p>先记住四条工程结论：</p><ul><li><strong>GC Roots 是判活的唯一入口</strong>：虚拟机栈局部变量、静态变量、常量、JNI 引用等能直接摸到的对象是&quot;根&quot;，顺着根往下爬，爬不到的就是垃圾。</li><li><strong>引用计数法在 JVM 不用</strong>：互相引用会成环导致永远不回收，HotSpot 用的是可达性分析 + 三色标记。</li><li><strong>分代收集是事实标准</strong>：新对象大多&quot;朝生夕死&quot;，所以堆被切成新生代（Eden + 2 个 Survivor）和老年代，用不同策略分别处理。</li><li><strong>STW（Stop The World）不可避免，但可以被压缩</strong>：标记起点、重新标记等阶段必须暂停用户线程，现代收集器把最耗时的遍历做成与应用并发。</li></ul><blockquote><p>版本基线：本文以 <strong>HotSpot / OpenJDK</strong> 为基线，默认 GC 行为按 <strong>JDK 9–21</strong> 说明，涉及 JDK 8 差异与 ZGC / Shenandoah 状态均已标注。事实核对日期：<strong>2026-08-19</strong>。主要参考：<a href="https://openjdk.org/jeps/248" target="_blank" rel="noreferrer">JEP 248 Make G1 the Default GC</a>、<a href="https://openjdk.org/jeps/377" target="_blank" rel="noreferrer">JEP 377 ZGC Production</a>、<a href="https://openjdk.org/jeps/379" target="_blank" rel="noreferrer">JEP 379 Shenandoah Production</a>、<a href="https://openjdk.org/jeps/439" target="_blank" rel="noreferrer">JEP 439 Generational ZGC</a>。</p></blockquote><h2 id="一、判定对象死活-两条路-jvm-走第二条" tabindex="-1">一、判定对象死活：两条路，JVM 走第二条 <a class="header-anchor" href="#一、判定对象死活-两条路-jvm-走第二条" aria-label="Permalink to “一、判定对象死活：两条路，JVM 走第二条”">​</a></h2><h3 id="_1-1-引用计数法-不用" tabindex="-1">1.1 引用计数法（不用） <a class="header-anchor" href="#_1-1-引用计数法-不用" aria-label="Permalink to “1.1 引用计数法（不用）”">​</a></h3><p>给每个对象挂一个计数器，被引用 +1、取消引用 -1，归零即可回收。</p><ul><li>优点：判定快、回收及时，不需要全局扫描。</li><li>致命缺点：<strong>循环引用</strong>。A 引用 B、B 引用 A，彼此计数器都不为 0，但外部已没有任何人能摸到它们，永远不会被回收。</li></ul><p>Python、Objective-C 用引用计数（配合逃逸分析/弱引用解决环），但 <strong>JVM 不采用</strong>。</p><h3 id="_1-2-可达性分析-jvm-采用" tabindex="-1">1.2 可达性分析（JVM 采用） <a class="header-anchor" href="#_1-2-可达性分析-jvm-采用" aria-label="Permalink to “1.2 可达性分析（JVM 采用）”">​</a></h3><p>从一组 <strong>GC Roots</strong> 出发，沿着引用关系向下搜索，凡是能连上的对象标记为&quot;存活&quot;，搜不到的就是要回收的垃圾。</p><p>可作为 GC Roots 的对象：</p><table tabindex="0"><thead><tr><th>来源</th><th>说明</th></tr></thead><tbody><tr><td>虚拟机栈（栈帧局部变量表）中引用的对象</td><td>正在执行的方法里 <code>new</code> 出来的局部变量</td></tr><tr><td>本地方法栈中 JNI（Native 方法）引用的对象</td><td>JNI 调用持有的 Java 对象</td></tr><tr><td>方法区中<strong>类静态属性</strong>引用的对象</td><td><code>static</code> 字段</td></tr><tr><td>方法区中<strong>常量</strong>引用的对象</td><td><code>static final</code> 常量</td></tr></tbody></table><p>可达性分析解决了循环引用问题，但它有一个代价：<strong>扫描整个对象图时必须 STW</strong>，否则用户线程一边改引用、GC 一边标，标记结果就不准。这正是&quot;三色标记&quot;要优化的点。</p><h3 id="_1-3-三色标记-把-全堆-stw-扫描-拆成并发" tabindex="-1">1.3 三色标记：把&quot;全堆 STW 扫描&quot;拆成并发 <a class="header-anchor" href="#_1-3-三色标记-把-全堆-stw-扫描-拆成并发" aria-label="Permalink to “1.3 三色标记：把&quot;全堆 STW 扫描&quot;拆成并发”">​</a></h3><p>三色标记把对象分成三种状态：</p><ul><li><strong>白色</strong>：还没被标记（潜在垃圾）。</li><li><strong>灰色</strong>：自己标了，但引用的对象还没标完（&quot;正在处理中&quot;）。</li><li><strong>黑色</strong>：自己和所有引用都标完了（确定存活）。</li></ul><p>流程：</p><ol><li><strong>初始标记</strong>（STW，极短）：从 GC Roots 直接摸到的对象标灰，只扫根，不扫全堆。</li><li><strong>并发标记</strong>（不 STW）：从灰色对象出发遍历对象图，标灰新碰到的、标黑处理完的。这一步最耗时，但和应用线程<strong>并发</strong>跑。</li><li><strong>重新标记</strong>（STW，很短）：修正并发期间应用线程改动的引用，把漏标的补上。</li></ol><blockquote><p>难点：<strong>并发标记期间应用会改对象图</strong>，可能产生&quot;漏标&quot;（本该黑的对象被挪走引用，变成白）。JVM 用<strong>写屏障（Write Barrier）</strong>在引用赋值那一刻记录变化，重新标记阶段兜底。CMS / G1 的&quot;增量更新&quot;与&quot;原始快照（SATB）&quot;就是两种写屏障策略。</p></blockquote><h2 id="二、四种引用-决定-多舍不得放你走" tabindex="-1">二、四种引用：决定&quot;多舍不得放你走&quot; <a class="header-anchor" href="#二、四种引用-决定-多舍不得放你走" aria-label="Permalink to “二、四种引用：决定&quot;多舍不得放你走&quot;”">​</a></h2><p>可达性分析回答&quot;能不能摸到&quot;，引用类型回答&quot;摸到了但有多不想扔&quot;。这四种引用决定了对象在内存吃紧时的命运：</p><table tabindex="0"><thead><tr><th>引用类型</th><th>回收时机</th><th>典型用途</th></tr></thead><tbody><tr><td><strong>强引用</strong> <code>Object o = new Object()</code></td><td>只要强引用在，绝不回收</td><td>普通对象，99% 场景</td></tr><tr><td><strong>软引用</strong> <code>SoftReference</code></td><td>内存将溢出前，先回收软引用对象；还不够才抛 OOM</td><td>内存敏感的缓存（如图片缓存）</td></tr><tr><td><strong>弱引用</strong> <code>WeakReference</code></td><td>下次 GC 无论内存是否充足都回收</td><td><code>ThreadLocal</code> 的 <code>Entry</code>、WeakHashMap</td></tr><tr><td><strong>虚引用</strong> <code>PhantomReference</code></td><td>必须配合引用队列，回收时入队，由 <code>ReferenceHandler</code> 线程释放堆外内存</td><td>管理 NIO 的 <code>DirectByteBuffer</code> 直接内存</td></tr></tbody></table><p><strong>真实示例</strong>：<code>ThreadLocal</code> 之所以用弱引用包住 <code>value</code>，是为了在业务代码忘记 <code>remove()</code> 时，至少 key 能被 GC 掉，减少内存泄漏面（但 value 仍可能被强引用链拖住，所以用完一定要 <code>remove()</code>）。</p><div class="language-java line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// ThreadLocal 的典型正确用法：用完即清理，避免线程池场景下 value 长期持有</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">private</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> static</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> final</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> ThreadLocal&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">SimpleDateFormat</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt; TL </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> ThreadLocal.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">withInitial</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(SimpleDateFormat</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">::new</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">);</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">void</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> handle</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">() {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    try</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        SimpleDateFormat sdf </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> TL.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">get</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">();</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">        // ... 使用 sdf 做格式化</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">finally</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        TL.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">remove</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(); </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// 关键：线程池线程会被复用，不 remove 会让 value 滞留在线程里</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br></div></div><h2 id="三、三种回收算法-碎片、空间、停顿的三角权衡" tabindex="-1">三、三种回收算法：碎片、空间、停顿的三角权衡 <a class="header-anchor" href="#三、三种回收算法-碎片、空间、停顿的三角权衡" aria-label="Permalink to “三、三种回收算法：碎片、空间、停顿的三角权衡”">​</a></h2><p>标记出垃圾后，怎么把内存收回来？三种基础算法各有取舍。</p><h3 id="_3-1-标记-清除-mark-sweep" tabindex="-1">3.1 标记-清除（Mark-Sweep） <a class="header-anchor" href="#_3-1-标记-清除-mark-sweep" aria-label="Permalink to “3.1 标记-清除（Mark-Sweep）”">​</a></h3><p>先标死活，再把死的清掉，活的原地不动。</p><ul><li>优点：简单，不需要移动对象。</li><li>缺点：<strong>内存碎片</strong>。清完一堆空洞，下次分配大对象时连续空间不够，反而触发 Full GC；执行效率不稳定，和对象数量相关。</li></ul><h3 id="_3-2-标记-复制-copying" tabindex="-1">3.2 标记-复制（Copying） <a class="header-anchor" href="#_3-2-标记-复制-copying" aria-label="Permalink to “3.2 标记-复制（Copying）”">​</a></h3><p>把存活对象整批拷到另一块空白区，原区域整体清空。</p><ul><li>优点：<strong>无碎片</strong>，分配只需指针碰撞，极快。</li><li>缺点：<strong>要两倍内存</strong>（或至少一块闲置的&quot;to&quot;区）。</li></ul><h3 id="_3-3-标记-整理-mark-compact" tabindex="-1">3.3 标记-整理（Mark-Compact） <a class="header-anchor" href="#_3-3-标记-整理-mark-compact" aria-label="Permalink to “3.3 标记-整理（Mark-Compact）”">​</a></h3><p>标记后，把存活对象往一端<strong>平移压缩</strong>，腾出连续空间，再把边界外清空。</p><ul><li>优点：空间连续，<strong>无碎片</strong>，不需要额外内存。</li><li>缺点：移动存活对象并更新所有引用是重活，必须<strong>全程 STW</strong>。</li></ul><blockquote><p>工程映射：新生代对象&quot;死得多、活得少&quot;，适合<strong>复制</strong>（Eden 复制到 Survivor）；老年代对象&quot;活得久&quot;，不适合频繁复制，多用<strong>标记-整理</strong>（或 CMS 的标记-清除 + 碎片问题）。</p></blockquote><h2 id="四、分代模型-为什么堆要切成几块" tabindex="-1">四、分代模型：为什么堆要切成几块 <a class="header-anchor" href="#四、分代模型-为什么堆要切成几块" aria-label="Permalink to “四、分代模型：为什么堆要切成几块”">​</a></h2><p><strong>弱分代假说</strong>：绝大多数对象熬不过第一次 GC。基于这个观察，堆被分为：</p><ul><li><strong>新生代（Young）</strong>：新对象先来这。由一块 <strong>Eden</strong> + 两块 <strong>Survivor（from / to）</strong> 组成，默认比例 <strong>8 : 1 : 1</strong>（<code>-XX:SurvivorRatio=8</code>）。 <ul><li>一次 Minor GC：Eden 和 from 里存活的对象，被<strong>复制</strong>到 to 区；年龄 +1；from / to 角色互换。</li><li>年龄到阈值（默认 15，<code>-XX:MaxTenuringThreshold</code>）的对象<strong>晋升老年代</strong>。</li></ul></li><li><strong>老年代（Old）</strong>：存长生命周期对象。满了就触发 <strong>Major / Full GC</strong>，通常更慢、停顿更长。</li><li><strong>永久代 / 元空间（Metaspace）</strong>：JDK 8 起方法区实现为<strong>元空间</strong>，放在<strong>本地内存</strong>而非堆里，存类元数据。用 <code>-XX:MaxMetaspaceSize</code> 限上限。</li></ul><p><strong>分配担保</strong>：Minor GC 后存活对象若 Survivor 放不下，就由老年代&quot;担保&quot;直接进老年代。所以大对象（如大数组）常通过 <code>-XX:PretenureSizeThreshold</code> 直接进老年代，避免 Eden 被瞬间占满。</p><h2 id="五、常见收集器-吞吐优先-vs-停顿优先" tabindex="-1">五、常见收集器：吞吐优先 vs 停顿优先 <a class="header-anchor" href="#五、常见收集器-吞吐优先-vs-停顿优先" aria-label="Permalink to “五、常见收集器：吞吐优先 vs 停顿优先”">​</a></h2><p>收集器是&quot;算法 + 分代 + 停顿策略&quot;的组合。按目标分两类：</p><h3 id="_5-1-吞吐优先" tabindex="-1">5.1 吞吐优先 <a class="header-anchor" href="#_5-1-吞吐优先" aria-label="Permalink to “5.1 吞吐优先”">​</a></h3><table tabindex="0"><thead><tr><th>收集器</th><th>作用代</th><th>算法</th><th>特点</th><th>适用</th></tr></thead><tbody><tr><td><strong>Serial / Serial Old</strong></td><td>新 / 老</td><td>复制 / 整理</td><td>单线程，GC 时全应用暂停</td><td>客户端、极小堆、嵌入式</td></tr><tr><td><strong>Parallel / Parallel Old</strong></td><td>新 / 老</td><td>复制 / 整理</td><td>多线程并行，吞吐最高</td><td>后台计算、批处理</td></tr></tbody></table><blockquote><p><strong>版本事实</strong>：<strong>JDK 8 的默认收集器是 Parallel GC（UseParallelGC）</strong>。从 <strong>JDK 9 起 G1 成为默认</strong>（JEP 248），因为对多数服务而言&quot;控停顿&quot;比&quot;榨吞吐&quot;更重要。</p></blockquote><h3 id="_5-2-停顿优先-低延迟" tabindex="-1">5.2 停顿优先（低延迟） <a class="header-anchor" href="#_5-2-停顿优先-低延迟" aria-label="Permalink to “5.2 停顿优先（低延迟）”">​</a></h3><table tabindex="0"><thead><tr><th>收集器</th><th>关键机制</th><th>停顿</th><th>代价</th><th>状态</th></tr></thead><tbody><tr><td><strong>CMS</strong></td><td>并发标记清除</td><td>初始/重新标记短暂停，清除并发</td><td>碎片、浮动垃圾、退化 Serial Old</td><td>已废弃（JDK 9 标记废弃，JDK 14 移除）</td></tr><tr><td><strong>G1</strong></td><td>把堆切成 Region，按&quot;回收收益&quot;优先清最有价值的区</td><td>可预测停顿模型（<code>-XX:MaxGCPauseMillis</code>）</td><td>吞吐略低于 Parallel</td><td><strong>JDK 9+ 默认</strong>，服务端主力</td></tr><tr><td><strong>ZGC</strong></td><td>着色指针 + 读屏障，全程并发</td><td><strong>亚毫秒级（&lt;1ms）</strong>，与堆大小无关</td><td>吃 CPU、需更大堆</td><td><strong>JDK 15 生产可用</strong>（JEP 377），分代 ZGC 于 <strong>JDK 21</strong>（JEP 439，<code>-XX:+UseZGC -XX:+ZGenerational</code>）</td></tr><tr><td><strong>Shenandoah</strong></td><td>转发指针 + 读屏障，并发整理</td><td>亚毫秒级，与堆大小无关</td><td>每个对象多一个转发指针，吃内存/CPU</td><td><strong>JDK 15 生产可用</strong>（JEP 379），Red Hat 主导</td></tr></tbody></table><blockquote><p><strong>选型速记</strong>：没有&quot;最好的 GC&quot;，只有&quot;最合适的&quot;。G1 是绝大多数服务的稳妥默认；对延迟极其敏感（如风控、交易网关，要求停顿稳定 &lt;10ms）才上 ZGC / Shenandoah，并准备好更多内存与 CPU。分代 ZGC 在 JDK 21+ 已是多数低延迟场景的更优解。</p></blockquote><h2 id="六、真实示例-spring-boot-服务的-gc-调优骨架" tabindex="-1">六、真实示例：Spring Boot 服务的 GC 调优骨架 <a class="header-anchor" href="#六、真实示例-spring-boot-服务的-gc-调优骨架" aria-label="Permalink to “六、真实示例：Spring Boot 服务的 GC 调优骨架”">​</a></h2><p>一个常见的后端服务启动参数骨架（JDK 17/21，G1 默认）：</p><div class="language-bash line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 堆大小：生产务必显式设定上下限，避免默认按物理内存比例膨胀</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">-Xms4g</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -Xmx4g</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 元空间限上限，防止类加载泄露把本地内存吃满</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">-XX:MaxMetaspaceSize</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">=256m</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 出现 OOM 时自动 dump 堆，便于事后分析（强烈建议开启）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">-XX:+HeapDumpOnOutOfMemoryError</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">-XX:HeapDumpPath</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">=/var/log/app/heapdump.hprof</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 开启统一 GC 日志（JDK 9+ 的 -Xlog 语法），保留滚动日志</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">-Xlog:gc*,gc+heap</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">=info,gc+age=trace:file=/var/log/app/gc.log:time,tags,level:filecount=10,filesize=50M</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 若延迟敏感且堆/CPU 充裕，可切 ZGC（JDK 21+ 分代）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># -XX:+UseZGC -XX:+ZGenerational</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br></div></div><p>GC 日志里最该盯的三类信号：</p><ul><li><strong>GC 频次</strong>：Young GC 每秒多次、Full GC 频繁 → 多半是对象晋升太快或堆太小。</li><li><strong>停顿时长</strong>：G1 的 <code>-XX:MaxGCPauseMillis</code> 只是&quot;目标&quot;，不是保证；若长期超标，考虑增大堆或换 ZGC。</li><li><strong>晋升失败 / 分配担保失败</strong>：老年代跟不上新生代节奏，会触发代价高昂的 Full GC。</li></ul><h2 id="七、常见追问-面试与排障高频" tabindex="-1">七、常见追问（面试与排障高频） <a class="header-anchor" href="#七、常见追问-面试与排障高频" aria-label="Permalink to “七、常见追问（面试与排障高频）”">​</a></h2><p><strong>Q1：为什么 GC 一定要 STW？</strong> 标记必须看到&quot;对象图某一刻的稳定快照&quot;，否则并发修改会让标记永远追不平。现代收集器把必须 STW 的环节（初始标记、重新标记）压到极短，把耗时遍历改成并发。</p><p><strong>Q2：为什么会有 Full GC，它为什么慢？</strong> Full GC 回收整个堆（含老年代），老年代用标记-整理，要移动对象并更新全部引用，必然长时间 STW。常见诱因：老年代碎片/空间不足、元空间不足、<code>System.gc()</code> 显式触发、晋升失败。</p><p><strong>Q3：三色标记的&quot;漏标&quot;怎么补？</strong> 靠写屏障记录并发期间的引用变更（CMS 用增量更新，G1 用 SATB 原始快照），在重新标记阶段把这些对象重新标黑。所以重新标记虽 STW，但通常很短。</p><p><strong>Q4：CMS 的&quot;浮动垃圾&quot;是什么？</strong> 并发清除阶段，应用线程仍在产生新垃圾（已死但还没被标），这些要等下一次 GC 才能清，叫浮动垃圾。因此不能等老年代 100% 满才触发 CMS，要留余量。</p><h2 id="八、踩坑-这些-oom-你迟早会遇" tabindex="-1">八、踩坑：这些 OOM 你迟早会遇 <a class="header-anchor" href="#八、踩坑-这些-oom-你迟早会遇" aria-label="Permalink to “八、踩坑：这些 OOM 你迟早会遇”">​</a></h2><table tabindex="0"><thead><tr><th>错误</th><th>含义</th><th>常见成因</th></tr></thead><tbody><tr><td><code>Java heap space</code></td><td>堆满</td><td>对象不释放、缓存无上限、堆太小</td></tr><tr><td><code>Metaspace</code></td><td>元空间满</td><td>动态生成类过多（CGLIB、Groovy、反射代理）</td></tr><tr><td><code>GC Overhead limit exceeded</code></td><td>GC 时间 &gt;98% 但回收极少</td><td>内存泄漏，对象都还&quot;活着&quot;</td></tr><tr><td><code>Direct buffer memory</code></td><td>堆外直接内存满</td><td>NIO <code>DirectByteBuffer</code> 用太多，未受 <code>-Xmx</code> 约束</td></tr><tr><td><code>Unable to create new native thread</code></td><td>线程数到顶</td><td>线程池失控、系统 <code>ulimit</code> 太小</td></tr></tbody></table><p><strong>典型坑</strong>：本地内存（元空间、直接内存、线程栈）<strong>不归 <code>-Xmx</code> 管</strong>。只调大堆，结果元空间或直接内存把宿主机拖垮——容器里尤其要注意设 <code>-XX:MaxMetaspaceSize</code> 和 <code>-XX:MaxDirectMemorySize</code>。</p><p><strong>另一个坑</strong>：<code>System.gc()</code> 看似&quot;手动清理&quot;，实则触发 Full GC，在延迟敏感服务里是性能地雷。很多框架的&quot;显式释放&quot;最终落到它身上，生产环境常加 <code>-XX:+DisableExplicitGC</code> 关掉。</p><h2 id="九、选择建议-先问业务要什么" tabindex="-1">九、选择建议：先问业务要什么 <a class="header-anchor" href="#九、选择建议-先问业务要什么" aria-label="Permalink to “九、选择建议：先问业务要什么”">​</a></h2><ul><li><strong>吞吐优先</strong>（离线计算、批处理、报表）：Parallel GC，给足 CPU 和堆，别纠结停顿。</li><li><strong>均衡通用</strong>（绝大多数 Web 服务、微服务）：<strong>G1 默认</strong>，调好 <code>-Xms=-Xmx</code>、<code>MaxGCPauseMillis</code>、开 GC 日志。</li><li><strong>低延迟敏感</strong>（交易、风控、实时推荐，要求停顿稳定亚十毫秒）：JDK 21+ 上 <strong>分代 ZGC</strong>；堆和 CPU 要预留余量（ZGC 用并发换延迟，吃资源）。</li><li><strong>大堆且怕碎片</strong>：避开 CMS（已移除）；G1 / ZGC 都会整理，优先 ZGC。</li></ul><h2 id="十、最佳实践清单" tabindex="-1">十、最佳实践清单 <a class="header-anchor" href="#十、最佳实践清单" aria-label="Permalink to “十、最佳实践清单”">​</a></h2><ol><li><strong>显式设定 <code>-Xms</code> 与 <code>-Xmx</code> 相等</strong>，避免运行期堆伸缩带来的额外 GC。</li><li><strong>永远开 <code>-XX:+HeapDumpOnOutOfMemoryError</code></strong>，出事才有现场。</li><li><strong>用 <code>-Xlog:gc*</code> 落 GC 日志</strong>，配滚动，排障先看频次和停顿。</li><li><strong>容器里设 <code>MaxMetaspaceSize</code> / <code>MaxDirectMemorySize</code></strong>，本地内存不归 <code>-Xmx</code> 管。</li><li><strong>别滥用 <code>System.gc()</code></strong>，延迟敏感服务可 <code>-XX:+DisableExplicitGC</code>。</li><li><strong>对象尽量短命</strong>：缩小作用域、用完 <code>close()</code> / <code>remove()</code>，让对象在新生代就死掉，少晋升老年代。</li><li><strong>监控优先于调参</strong>：在没看到 GC 日志和堆走势前，不要盲目换收集器或堆大小。</li></ol><h2 id="总结" tabindex="-1">总结 <a class="header-anchor" href="#总结" aria-label="Permalink to “总结”">​</a></h2><p>GC 的本质是一道题：<strong>顺着 GC Roots 引用链，判定哪些对象还活着，再用合适的算法把它们之外的内存收回来</strong>。可达性分析 + 三色标记解决了&quot;判活且尽量并发&quot;，四种引用解决了&quot;多舍不得放&quot;，三种回收算法在&quot;碎片 / 空间 / 停顿&quot;间权衡，分代模型则利用&quot;对象朝生夕死&quot;的规律把问题拆小。落到选型：JDK 9+ 默认 G1 是稳妥解，吞吐看 Parallel，极致低延迟上 ZGC / Shenandoah——但任何选择都要先有 GC 日志和监控，再谈调优。</p><p>记住一句话：<strong>GC 不神秘，它是&quot;标记活的、清掉死的、把活的压紧&quot;的房间打扫；小黑推着清扫车进堆里，沿根引用把活对象留下，把死对象扫走，再把活对象挪到一侧腾出连续空间——停顿，就是它必须让所有人站定那一下的瞬间。</strong></p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("posts/jvm-gc-garbage-collection-engineering.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const jvmGcGarbageCollectionEngineering = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  jvmGcGarbageCollectionEngineering as default
};
